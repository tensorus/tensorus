# This file provides an example of environment variables that can be used by Tensorus.
# Copy this file to .env and fill in the values for your environment.

# --- NQL Agent LLM Configuration ---
# Optional: API key for using a Gemini LLM with the NQLAgent.
# If NQL_USE_LLM is true and this key is not provided, NQLAgent may fail to initialize the LLM.
GEMINI_API_KEY=""

# Optional: Specifies the LLM model to be used by the NQLAgent.
# Defaults to "gemini-pro" if not set, but LLMParser is active.
NQL_LLM_MODEL="gemini-pro"

# Optional: Set to "true", "1", or "yes" to enable LLM features in NQLAgent if an API key is available.
# NQL_USE_LLM="false"
