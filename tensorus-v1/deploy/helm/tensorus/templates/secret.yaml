apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tensorus.fullname" . }}-auth
  labels:
    {{- include "tensorus.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.auth.apiKey | quote }}
