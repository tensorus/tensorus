from setuptools import setup, find_packages

setup(
    name="tensorus",
    version="0.0.1",
    description="A agentic tensor database for tensor data.",
    author="Tensorus",
    packages=find_packages(),
    install_requires=[],
    license="MIT",
)
