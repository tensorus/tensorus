# Core dependencies for Tensorus
torch>=1.13.0
numpy>=1.21.0

# Dependencies for Ingestion Agent examples
Pillow>=9.0.0

# Dependencies for API Layer
fastapi>=0.90.0
uvicorn[standard]>=0.20.0 # Includes standard dependencies like watchfiles
pydantic>=1.10.0,<2.0.0 # FastAPI often has specific Pydantic v1 compatibility, lock for safety

# Optional for potential future file uploads via API
# python-multipart>=0.0.5